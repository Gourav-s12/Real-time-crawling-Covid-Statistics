Write the required country's name in the makefile to get its percent data compared to the world's data.

Run Makefile using : make command in the terminal.

Note: Make sure the path provided to the text file is correct. The text.txt file is the the file generated from the module1 part.

The output is written in a text file named percentage.txt. 
